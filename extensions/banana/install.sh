#!/usr/bin/env bash
set -euo pipefail
echo "Banana support is already bundled with ctk-codex-seo."
echo "No files, settings, agents, packages, or credentials were changed."
echo "Configure GOOGLE_AI_API_KEY only when using the image workflow."
