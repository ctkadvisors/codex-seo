#!/usr/bin/env bash
set -euo pipefail
echo "DataForSEO support is already bundled with ctk-codex-seo."
echo "No files, settings, agents, packages, or credentials were changed."
echo "Use documented environment variables or ~/.config/ctk-codex-seo for credentials."
