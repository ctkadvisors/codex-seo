#!/usr/bin/env bash
set -euo pipefail
echo "Firecrawl support is already bundled with ctk-codex-seo."
echo "No files, settings, agents, packages, or credentials were changed."
echo "Configure FIRECRAWL_API_KEY only when using the Firecrawl workflow."
