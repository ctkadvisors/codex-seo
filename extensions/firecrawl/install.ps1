$ErrorActionPreference = "Stop"
Write-Host "Firecrawl support is already bundled with ctk-codex-seo."
Write-Host "No files, settings, agents, packages, or credentials were changed."
Write-Host "Configure FIRECRAWL_API_KEY only when using the Firecrawl workflow."
